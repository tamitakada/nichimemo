SPECIAL CHAR	ACTION				VALUE

/n		Separates bullets.
/n/n		Separates paragraphs.

{box_options}	Required metadata at start	{} [DEFAULT]
		of a new paragraph.		Any valid CSS rules applied to paragraph container.
						
						USE box_style KEY to apply custom style types.

{options}	JSON dict of CSS rules.		{} [DEFAULT]
		Required at the start of
		a new line.			Any valid CSS rules applied to entire line.
						Custom keys may be used to add behaviors.

						USE style KEY to apply custom style types.

<{options}text>	JSON dict of CSS rules.		{} [DEFAULT]
		Only applied to component.	Any valid CSS rules applied to component.
						Custom keys may be used to add behaviors.
						
						USE style KEY to apply custom style types.


==== CUSTOM KEYS WITH TEXT

KEY		VALUE			EFFECT

tt		string			tooltip

f		string			furigana

a		string			hyperlink

t		t (true)		paragraph
		f (false) [DEFAULT]	or list item

style		h			centered heading
		i			important: red highlight
		gtt			green glow indicating tooltip
		gp			red glow indicating important name
		kw			yellow color for keyword


==== CUSTOM STYLES USED WITH KEY "box_style"

CUSTOM VALUE	CUSTOM STYLE
i		ice back, navy front
s		sea border, white front